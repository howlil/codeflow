# Legacy Compatibility Pointer

Live execution state moved to `.agent/CURRENT_ITERATION.md`.

Use that file for:

```text
Feature Shape -> Current Position -> Delta -> Next Move
```

Do not maintain a second sprint/iteration state here. Historical milestone requirements remain in `.agent/requirements/product-roadmap.md` and durable architecture/design decisions remain in their owning specs/`DESIGN.md`.
